const express = require('express');
const passport = require('passport');

const router = express.Router();

// Optional: Admin Google OAuth entry point.
// This part is intentionally NOT enabled by default in server.js for Part 4.
// (Admin protection is handled by checking email's current role in callback.)

module.exports = router;

